FutureProvider.family를 사용하는 **이유는 딱 하나**야.

  

➡️ **“month, year 값이 매번 달라지는 동일 Provider를 만들기 위해서.”**

  

조금 더 정확하게 풀어줄게.

---

# **✅ 1. 왜 family를 쓰는가?**

  

### **✔** 

### **Provider에 파라미터를 전달하고 싶을 때 반드시 family가 필요함**

  

일반 Provider는 이렇게 생김:

```
final exampleProvider = FutureProvider((ref) async { ... });
```

- 인자를 받을 수 없음
    
- 따라서 month / year / teamId 같은 값에 따라 다른 데이터를 가져올 수 없음
    

  

하지만 지금 로직은:

```
month, year → 매번 다른 값이 들어와야 함
```

어떤 달을 보고 싶냐에 따라 매번 다른 API 요청을 보냄.

  

예:

- 2025년 12월 조회
    
- 2025년 11월 조회
    
- 2024년 08월 조회
    

  

이걸 하나의 Provider로 재활용하려면 **family**밖에 답이 없음.

---

# **✅ 2. family가 없으면 어떻게 되는가?**

  

예를 들어 family 없이 만들면:

```
final workTimesheetsProvider = FutureProvider((ref) async {
  ...
});
```

이제 아래 같은 호출이 불가능해져:

```
ref.watch(workTimesheetsProvider(month, year));
```

즉, month/year를 전달하지 못해서 “특정 달” 조회가 절대 불가능해짐.

---

# **✅ 3. family를 쓰면 얻는 효과**

  

### **✔️ Provider 호출 시 “인자 기반 캐싱” 제공**

  

예:

```
ref.watch(workTimesheetsProvider(12, 2025));
```

- 같은 인자를 넣으면 캐시된 데이터 재사용
    
- 다른 인자를 넣으면 새로운 Provider instance 생성
    
- Riverpod이 자동으로 구분해서 관리함
    

  

즉,

|**호출**|**Provider 구분**|
|---|---|
|workTimesheetsProvider(12, 2025)|A 인스턴스|
|workTimesheetsProvider(11, 2025)|B 인스턴스|

서로 다른 데이터 유지 가능.

---

# **✅ 4. family의 정확한 역할 요약**

- Provider 생성 시 **파라미터 전달 가능**
    
- 인자별로 **서로 다른 Provider 상태를 자동 분리**
    
- 인자를 키로 해서 **캐싱 & Rebuild 최적화**
    
- API 조회 시 각 날짜별로 요청 컨트롤 가능
    

---

# **📌 만약 family 안 쓰면 생기는 문제**

1. month/year를 전달할 수 없음
    
2. 어떤 달을 조회하는 Provider인지 알 수 없음
    
3. 매달 다른 데이터를 유지할 수 없음
    
4. 이전/다음 달 이동 시 Provider를 다시 선언해야 함 → 비효율적
    

---

# **✨ 결론**

  

FutureProvider.family를 사용하는 이유는

**특정 달(month, year)에 따라 다른 API 요청을 보내야 하기 때문이며,**

**이 값을 provider에 인자로 전달하기 위해서 반드시 family가 필요하기 때문**이야.

---

원하면:

- 이 Provider를 사용하는 위젯 구조
    
- 캐싱/로딩 최적화 방법
    
- month/year navigation 구조 개선
    
    까지 바로 설명해줄게.

[[flutter > record type]]